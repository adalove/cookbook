<div id="books">
<ol>
<% @books.each do |book| %>
  <ul>
    <li><%= link_to(book.title, book) %></li>
    <li><%= book.cuisine %></li>
    <li><%= book.chef %></li>
    <li><%= image_tag(book.image) %></li>
  </ul>
<% end %>
</ol>
</div>